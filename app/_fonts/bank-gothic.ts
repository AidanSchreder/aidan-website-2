// Industrial title face carried over from v1. Used by engineering and 3D.
import localFont from "next/font/local";

export const bankGothic = localFont({
  src: "../../public/fonts/Bank Gothic Light Regular.otf",
  variable: "--font-title",
  display: "swap",
  weight: "400",
});
